# react-compare-products
Compare product features to each other with API feed

to start the application run these commands on terminal:

1. npm i
2. npm run start

To test;

npm run test

to build;

npm run build